import { FilterAvailablePipe } from './filter-available.pipe';

describe('FilterAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
